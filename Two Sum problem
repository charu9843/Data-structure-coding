class Solution {
public int[] twoSum(int[] nums, int target) {
    for(int i = 0; i < nums.length; i++){
        for(int j = i+1; j < nums.length; j++){
            if(nums[i] + nums[j] == target){
                return new int[]{i, j};
            }
        }
    }
    return new int[]{};
}
This is one kind of approach normal way of thinking logic  Time complexity is O(n²). Space complexity is O(1).”
Another way is hashmap approach the timecomplexity is O(n²) to O(n).”
